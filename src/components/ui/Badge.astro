---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="inline-flex w-fit items-center rounded-sm bg-white px-3 py-1 text-xs font-semibold uppercase tracking-normal text-[var(--color-civic-blue)] ring-1 ring-[var(--color-civic-line)]">
  {label}
</span>
